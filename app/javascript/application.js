// Configure your import map in config/importmap.rb
import "htmx.org"
import htmx from "htmx.org"
htmx.logAll()

import React from "react"
import ReactDOM from "react-dom"

console.log("hola", { React, ReactDOM })
